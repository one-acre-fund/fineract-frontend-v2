apiVersion: v1
kind: Service
metadata:
  name: fineract-v2-service
  namespace: paymenthub
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
  selector:
    app: fineract-frontend